# Kwork Backend API

API сервер для интернет-магазина памятников и ритуальных принадлежностей.

## 🚀 Быстрый старт

### Локальная разработка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env`:
```bash
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kwork_db
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
```

3. Запустите миграции:
```bash
npm run migrate
```

4. Заполните базу тестовыми данными:
```bash
npm run seed
```

5. Запустите сервер:
```bash
npm run dev
```

### Docker

1. Запустите с помощью Docker Compose:
```bash
docker-compose up -d
```

## 📡 API Endpoints

### Продукты
- `GET /api/monuments` - Получить все памятники
- `GET /api/fences` - Получить все ограды
- `GET /api/accessories` - Получить все аксессуары
- `GET /api/landscape` - Получить товары для благоустройства

### Готовые работы
- `GET /api/works` - Получить все работы
- `POST /api/works` - Создать новую работу
- `PUT /api/works/:id` - Обновить работу
- `DELETE /api/works/:id` - Удалить работу

### Контент
- `GET /api/epitaphs` - Получить эпитафии
- `GET /api/blogs` - Получить блоги
- `GET /api/campaigns` - Получить акции

### Файлы
- `GET /api/files/works` - Получить список изображений работ

## 🏗️ Деплой на хостинг

### 1. Подготовка базы данных

Создайте PostgreSQL базу данных на вашем хостинге и получите DATABASE_URL.

### 2. Настройка переменных окружения

Установите следующие переменные на вашем хостинге:
```
DATABASE_URL=postgresql://user:password@host:port/database
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://yourdomain.com
```

### 3. Деплой

#### Railway.app
```bash
# Подключите GitHub репозиторий
# Railway автоматически деплоит при push
```

#### Heroku
```bash
# Создайте приложение
heroku create kwork-api

# Добавьте PostgreSQL
heroku addons:create heroku-postgresql:hobby-dev

# Установите переменные
heroku config:set NODE_ENV=production
heroku config:set FRONTEND_URL=https://yourdomain.com

# Деплой
git push heroku main

# Запустите миграции
heroku run npm run migrate
heroku run npm run seed
```

#### DigitalOcean App Platform
1. Подключите GitHub репозиторий
2. Выберите папку `/backend` как source
3. Добавьте переменные окружения
4. Создайте PostgreSQL базу данных
5. Деплойте!

## 📁 Структура проекта

```
backend/
├── src/
│   ├── controllers/     # Контроллеры (будущее)
│   ├── middleware/      # Middleware (будущее)
│   ├── models/         
│   │   └── schema.js   # Схема базы данных
│   ├── routes/         # API маршруты
│   │   ├── works.js
│   │   ├── monuments.js
│   │   └── ...
│   ├── scripts/        # Скрипты миграции
│   │   ├── migrate.js
│   │   └── seed.js
│   ├── utils/
│   │   └── db.js       # Подключение к БД
│   └── index.js        # Главный файл сервера
├── .env               # Локальные переменные
├── .env.production    # Продакшн переменные
├── Dockerfile         # Docker конфигурация
└── package.json
```

## 🔧 Технологии

- **Node.js** + **Express.js** - Сервер
- **PostgreSQL** - База данных
- **Drizzle ORM** - ORM для работы с БД
- **Docker** - Контейнеризация
- **Helmet** - Безопасность
- **CORS** - Кросс-доменные запросы